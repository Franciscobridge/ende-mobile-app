import { View } from "react-native"
import { FixedHeader } from "../../components/custom/fixed-header"

export default function AddEnergy() {
  return (
    <View className="flex-1 bg-background">
      <FixedHeader title="Recarregar Energia" />
    </View>
  )
}
